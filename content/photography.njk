---
layout: layouts/base.njk
header: Photo
---

<section>
    <h1 class="centered">Photography</h1>
    <p>
        I'm a hobbyist photographer! These are my photo series.
    </p>
    <p>
        Subscribe to the <a href="feed.atom">photography specific atom feed</a>!
    </p>
</section>

<section>
    {% for series in collections.photography | reverse %}
        <div class="card">
            <h1><a href={{ series.url }}>{{series.data.title}}</a></h1>
            <time datetime="{{ series.data.date }}">{{ series.data.date | readableDate }}</time>
            {% if series.data.updated != null %}
                <time datetime="{{ series.data.updated }}">(updated {{ series.data.updated | readableDate }})</time>
            {% endif %}
            
            <br>
            {{series.data.description}}
        </div>
    {% endfor %}
</section>